{
  "name": "grp30_404",
  "private": true,
  "version": "1.0.0",
  "description": "Video Learning Hub — Groupe 30 Hackathon",
  "scripts": {
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "dev": "concurrently -n web,api -c magenta,cyan \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "dev:all": "npm run dev",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start:api": "npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.3"
  }
}
