# Ancien code minimal du repo (avant intégration Pôle 1)

- `frontend-old/` — CommentsPanel.jsx basique
- `backend-old/` — API commentaires Express (port 4000)

Conservé pour référence. Le code actif est dans `frontend/` et `backend/`.
